# Python 3.11 / 3.12
#
# A package is pinned once it has actually been installed and exercised. The
# unpinned ones below belong to build stages that do not exist yet; pinning a
# version nobody has run would be inventing a number rather than recording one.

# Backend
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart
sse-starlette==3.4.8

# Orchestration
langchain
langchain-openai
openai==3.0.0

# PDF processing — versions verified working on macOS / Python 3.12
docling==2.119.0
pymupdf==1.28.2

# OCR, used only when a PDF has no text layer. RapidOCR needs no system package,
# unlike Tesseract, so a clean clone works without brew or apt first.
rapidocr==3.9.2
onnxruntime==1.28.0

# Vector store
pymilvus[milvus_lite]==3.0.1

# UI
streamlit==1.61.1
requests==2.34.2

# Utilities
python-dotenv==1.2.2
tiktoken==0.13.0

# Tests and linting
pytest==9.1.1
httpx==0.28.1
ruff==0.15.22
